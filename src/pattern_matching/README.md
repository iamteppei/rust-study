- # Pattern Matching

- Pattern matching lets you check a value against a pattern and extract parts of it at the same time.
- `match` is exhaustive, so every possible case must be handled.
- Enum variants can carry different shapes of data, and pattern matching makes it easy to destructure them.
- Use `if let` when you only care about one specific pattern and want a shorter form than `match`.
