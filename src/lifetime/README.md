# Lifetime

- A lifetime is the scope for which a reference is valid.
- Rust uses lifetimes to prevent dangling references at compile time.
- When references are related, the compiler checks that the referenced data lives at least as long as the reference.

